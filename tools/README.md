# Tools

Scripts, cron configs, and agent configs live here.

**STATUS:** Pending cleanup — many scripts contain hardcoded API keys.
See task: `skill-cleanup-tools-001`

After cleanup, structure:
```
tools/
├── scripts/      # Executable scripts
├── cron/        # Cron job definitions
└── configs/     # Agent YAML configs
```
